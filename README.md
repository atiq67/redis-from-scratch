So far-
1.Networking	Implemented a custom TCP Server using raw Socket Programming. Managed the full socket lifecycle: socket(), bind(), listen(), and accept().
2.Protocol	Designed a Request-Response Protocol with a custom binary wire format. Implemented length-prefixed message framing to solve the "TCP stream fragmentation" problem.
